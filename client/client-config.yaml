# Claude Code Sessions Dashboard — Sync Client Configuration
server_url: "http://localhost:5050"
api_key: "change-me-to-a-secret"
# vm_name: "my-vm"  # unique label for this machine (defaults to hostname)
sync_interval: 3600  # seconds, default 1 hour (for daemon mode)

# Cloudflare Access service token (required if server is behind Cloudflare Access)
# cf_access_client_id: "xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx.access"
# cf_access_client_secret: "xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx"
