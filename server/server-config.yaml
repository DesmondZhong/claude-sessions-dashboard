# Claude Code Sessions Dashboard — Server Configuration
api_key: "change-me-to-a-secret"
host: "0.0.0.0"
port: 5050
debug: false
# db_path: /path/to/sessions.db  # defaults to ./sessions.db
# backup_dir: /path/to/backups   # defaults to ./backups (raw JSONL files)
