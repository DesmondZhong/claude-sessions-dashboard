# Claude Sessions Sync Agent — Configuration
server_url: "http://your-server:5000"
api_key: "change-me-to-a-secret"
vm_name: "my-vm"  # unique label for this machine
sync_interval: 300  # seconds (for daemon mode)
